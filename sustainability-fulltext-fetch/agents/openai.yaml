interface:
  display_name: "Sustainability Fulltext Fetch"
  short_description: "API-first DOI enrichment with webpage fallback"
  default_prompt: "Use $sustainability-fulltext-fetch to enrich relevant DOI rows in shared SQLite by calling OpenAlex first, Semantic Scholar fallback, then webpage fulltext fallback with retry-safe persistence."
