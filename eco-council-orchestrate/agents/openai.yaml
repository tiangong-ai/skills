interface:
  display_name: "Eco Council Orchestrate"
  short_description: "Eco-council staged orchestration and handoffs"
  default_prompt: "Use $eco-council-orchestrate to bootstrap eco-council runs, honor audited source selections, run the staged data plane, require moderator matching authorization before matching, and advance rounds safely."
