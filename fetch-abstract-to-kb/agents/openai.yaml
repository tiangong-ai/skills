interface:
  display_name: "Fetch Abstract to KB"
  short_description: "Fetch DOI rows and write abstracts"
  default_prompt: "Use $fetch-abstract-to-kb to fetch newest DOI rows with non-empty author and empty abstract, then batch write abstract values by DOI."
