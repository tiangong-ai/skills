interface:
  display_name: "Email IMAP Append"
  short_description: "Append draft emails with attachments to IMAP"
  default_prompt: "Use $email-imap-append to append an unsent draft email (optionally with attachments) to an IMAP Drafts mailbox via APPEND."
