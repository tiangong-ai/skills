interface:
  display_name: "Email IMAP Fetch"
  short_description: "Monitor inboxes and push to OpenClaw"
  default_prompt: "Use $email-imap-fetch to run an always-on IMAP listener for multiple env-configured accounts and forward each new email to OpenClaw webhooks."
